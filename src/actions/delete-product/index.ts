import { headers } from "next/headers"

import { auth } from "@/lib/auth"


export const Delete = async () => {

    const session = await auth.api.get()

    const DeleteProduct = () => {

    }

    const DeleteVariantProduct = () => {

    }
} 